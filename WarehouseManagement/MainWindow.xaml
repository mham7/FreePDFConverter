<Window x:Class="WarehouseManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseManagement"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Login" Margin="5"/>

        <TextBlock Grid.Row="1" Text="Email:" Margin="5"/>
        <TextBox Grid.Row="2" Name="EmailTextBox" Margin="5"/>

        <TextBlock Grid.Row="3" Text="Password:" Margin="5"/>
        <PasswordBox Grid.Row="4" Name="PasswordBox" Margin="5"/>

        <Button Grid.Row="3" Content="Login"  Margin="6"/>

        <TextBlock Grid.Row="4" Name="LoginResultText" Margin="5"/>
    </Grid>
</Window>
